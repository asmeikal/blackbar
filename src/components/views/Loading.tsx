import React from 'react';
import { View, Text } from 'react-native';

export const Loading = () => (
    <View
    style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    }}
    >
        <Text>Blackbar</Text>
    </View>
);
